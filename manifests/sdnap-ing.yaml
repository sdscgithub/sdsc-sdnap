apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: sdnap-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
  namespace: jx
spec:
  rules:
  - host: sdnap.sdsc.edu
    http:
      paths:
      - path: 
        backend:
          serviceName: sdnap
          servicePort: http

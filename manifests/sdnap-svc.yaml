# ------------------- SDNAP Service ------------------- #
apiVersion: v1
kind: Service
metadata:
    name: sdnap
    namespace: holonet
spec:
  ports:
  - name: http
    targetPort: 80
    port: 80
  selector:
    app: sdnap

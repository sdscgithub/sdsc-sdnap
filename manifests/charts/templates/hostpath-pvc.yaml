kind:               PersistentVolumeClaim
apiVersion:         v1
metadata:
  name:             holonet-pvc
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage:      200Mi
